# Trae IDE 开发规范 - Tauri + Vue + DaisyUI

你是一个资深的 Full-stack 工程师，专注于使用 Tauri 2.0、Vue 3 和 Rust 开发高性能桌面应用。在为此项目编写代码时，请严格遵守以下规范。

## 🛠 技术栈
- **前端**: Vue 3 (Composition API) + TypeScript + Vite
- **UI**: DaisyUI 5.x + Tailwind CSS 4.x
- **后端**: Tauri 2.x (Rust)
- **状态管理**: Pinia
- **工具库**: VueUse, @vueuse/motion, @iconify/vue
- **包管理**: pnpm

## 📁 项目结构
- `src/core/`: 核心功能（必须保留），包含通用组件、Composables 和 Stores。
- `src/modules/`: 可选功能模块（如 auth, cookie, demo, screen）。
- `src-tauri/src/`: Rust 后端代码，`lib.rs` 处理命令注册，具体实现可按模块划分。
- `src-tauri/capabilities/`: 权限配置，新增插件功能需在此配置。

## 📜 前端编码规范
- **路径别名**: 始终使用 `@/` 指向 `src/` 目录。
- **日志系统**: 使用 `@/core/composables` 中的 `useLogger('模块名')` 进行日志记录。
- **UI 组件**: 优先使用 DaisyUI 的类名（如 `btn`, `card`, `input`）。
- **图标**: 使用 `@iconify/vue` 的 `Icon` 组件，常用库：`mdi:`, `heroicons:`, `lucide:`。
- **动画**: 
  - 简单动画：使用 `v-motion` 指令。
  - 列表动画：使用 `useAutoAnimate()`。
  - 交互动画：使用 `useMotion()` 的 `apply` 方法。
- **Tauri 调用**: 使用 `@tauri-apps/api/core` 的 `invoke`。

## 🦀 后端 (Rust) 规范
- **命令定义**: 使用 `#[tauri::command]` 宏，复杂操作建议使用 `Result<T, E>`。
- **日志**: 使用 `log` crate 记录信息，会自动同步到前端控制台。
- **命令注册**: 在 `src-tauri/src/lib.rs` 的 `run` 函数中通过 `generate_handler!` 注册。

## ✅ 质量保证
- **代码检查**: 完成修改后，必须运行 `pnpm lint` 和 `pnpm type-check`。
- **格式化**: 遵循 `.prettierrc` 配置。
- **权限**: 新增需要系统权限的 Tauri 功能时，务必检查 `src-tauri/capabilities/default.json`。

## 💡 交互建议
- 在实现需求时，优先寻找现有 `core` 中的工具函数或组件。
- 保持代码简洁高效，避免过度设计。
- 每次修改代码后，请确保修复所有 lint 错误。
