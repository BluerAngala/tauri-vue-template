# Trae 开发指南 - Tauri + Vue + DaisyUI

本文档是 Trae IDE 的专用开发指南，旨在帮助 AI 更好地理解和协助本项目的开发。

## 🛠 项目技术栈

- **前端框架**: Vue 3 + TypeScript
- **UI 组件库**: DaisyUI 5.x (基于 Tailwind CSS 4.x)
- **桌面框架**: Tauri 2.x (Rust 后端)
- **构建工具**: Vite + Rolldown
- **状态管理**: Pinia
- **工具库**: VueUse, @vueuse/motion, @formkit/auto-animate, @iconify/vue

## 📂 核心目录结构

- `src/core/`: 核心通用逻辑（Logger, Theme, Toast, App Store）。
- `src/modules/`: 功能模块化目录，新增功能应在此创建新文件夹。
- `src-tauri/src/`: Rust 后端代码。

## 💻 开发规范

### 1. 前端规范
- **别名**: 始终使用 `@/`。
- **日志**: 必须使用 `useLogger`。
- **图标**: 优先使用 `Icon` 组件。
- **样式**: 严格遵循 DaisyUI 和 Tailwind CSS 类名。

### 2. 后端 (Rust) 规范
- **命令**: 必须在 `lib.rs` 中注册。
- **权限**: 必须在 `capabilities/default.json` 中配置。

## 🚀 常用命令
- `pnpm tauri dev`: 启动开发环境。
- `pnpm lint`: 执行代码检查并自动修复。
- `pnpm type-check`: 执行类型检查。
